I t hought in another session we had converted the prose in the demos to handlebars like injections with the demo carrying its own i18n json and then injecting the strings at runtime- allowing the translations workflow to use the same JSON parsing as the other scripts.

---

let's do it, and then as part of that pass configure the verbatim settings for the prose in the JSON templates.

---

if we're going to do a separate i18n file then let's re-organize the demos folder into:
demos/<component>/index.html
demos/<component>/i18n.json

---

[Terminal 26e13336-7841-40fd-984e-1492fdfd1f50 notification: command completed. The terminal has been cleaned up.]
Terminal output:
  ! text unit looks untranslated (identical to source), not cached: "small print"
  ! text unit looks untranslated (identical to source), not cached: ", and some "
  ! text unit looks untranslated (identical to source), not cached: ". The prose layer styles rendered\n      "
  ! text unit looks untranslated (identical to source), not cached: "A blockquote, styled from the Text conte"
  ! text unit looks untranslated (identical to source), not cached: "Bucket"
  ! text unit looks untranslated (identical to source), not cached: "@pantoken/css"
  ! text unit looks untranslated (identical to source), not cached: "formats"
  ! text unit looks untranslated (identical to source), not cached: "@pantoken/react"
  ! text unit looks untranslated (identical to source), not cached: "renderers"
  ! text unit looks untranslated (identical to source), not cached: "Standard"
  ! text unit looks untranslated (identical to source), not cached: " Option A"
  ! text unit looks untranslated (identical to source), not cached: " Option B"
  ! text unit looks untranslated (identical to source), not cached: " Large"
  ! text unit looks untranslated (identical to source), not cached: "Toggle buttons (variant=\"toggle\")"
  ! text unit looks untranslated (identical to source), not cached: " Medium"
  ! text unit looks untranslated (identical to source), not cached: "Toggle context colors"
  ! text unit looks untranslated (identical to source), not cached: " Success"
  ! text unit looks untranslated (identical to source), not cached: " Danger"
  ! text unit looks untranslated (identical to source), not cached: " Warning"
  ! text unit looks untranslated (identical to source), not cached: " Off"
  ! text unit looks untranslated (identical to source), not cached: "T-shirt size"
  ! text unit looks untranslated (identical to source), not cached: " Small\n      "
  ! text unit looks untranslated (identical to source), not cached: " Medium\n      "
  ! text unit looks untranslated (identical to source), not cached: " Large "
  ! text unit looks untranslated (identical to source), not cached: "Notification frequency"
  ! text unit looks untranslated (identical to source), not cached: " Real time"
  ! text unit looks untranslated (identical to source), not cached: " Daily digest"
  ! text unit looks untranslated (identical to source), not cached: " Weekly digest"
  ! text unit looks untranslated (identical to source), not cached: "Contact method"
  ! text unit looks untranslated (identical to source), not cached: " Email"
  ! text unit looks untranslated (identical to source), not cached: " Phone"
  ! text unit looks untranslated (identical to source), not cached: " Mail"
  ! text unit looks untranslated (identical to source), not cached: "Grading range"
  ! text unit looks untranslated (identical to source), not cached: "\n      Narrow layout — hidden at "
  ! text unit looks untranslated (identical to source), not cached: " (48rem) and up via "
  ! text unit looks untranslated (identical to source), not cached: "\n      Wide layout — hidden at "
  ! text unit looks untranslated (identical to source), not cached: " and below via "
  ! text unit looks untranslated (identical to source), not cached: "\n      Breakpoints: "
  ! text unit looks untranslated (identical to source), not cached: " 30rem, "
  ! text unit looks untranslated (identical to source), not cached: " 48rem, "
  ! text unit looks untranslated (identical to source), not cached: " 64rem,\n      "
  ! text unit looks untranslated (identical to source), not cached: " 80rem. For a component that adapts to i"
  ! text unit looks untranslated (identical to source), not cached: " element and use\n      "
  ! text unit looks untranslated (identical to source), not cached: " queries with the same widths.\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n  This sentence is visible. After it si"
  ! text unit looks untranslated (identical to source), not cached: "\n    This text is announced to assistive"
  ! text unit looks untranslated (identical to source), not cached: "\n  Switch to the HTML tab to see it in t"
  ! text unit looks untranslated (identical to source), not cached: "Calendar"
  ! text unit looks untranslated (identical to source), not cached: "Page content sits beside the rail."
  ! text unit looks untranslated (identical to source), not cached: "Choose a fruit…"
  ! text unit looks untranslated (identical to source), not cached: "Apple"
  ! text unit looks untranslated (identical to source), not cached: "Orange"
  ! text unit looks untranslated (identical to source), not cached: "Pear"
  ! text unit looks untranslated (identical to source), not cached: "Invalid selection"
  ! text unit looks untranslated (identical to source), not cached: "Spacing"
  ! text unit looks untranslated (identical to source), not cached: "\n    Per-side classes on the spacing sca"
  ! text unit looks untranslated (identical to source), not cached: " for margin/padding, an\n    optional log"
  ! text unit looks untranslated (identical to source), not cached: "),\n    then a step ("
  ! text unit looks untranslated (identical to source), not cached: ", plus "
  ! text unit looks untranslated (identical to source), not cached: " for\n    margin). Every class also has a"
  ! text unit looks untranslated (identical to source), not cached: " is the same\n    rule as "
  ! text unit looks untranslated (identical to source), not cached: "p-xs, mb-2xs"
  ! text unit looks untranslated (identical to source), not cached: "p-sm, mb-2xs"
  ! text unit looks untranslated (identical to source), not cached: "p-lg"
  ! text unit looks untranslated (identical to source), not cached: "Center with auto margins"
  ! text unit looks untranslated (identical to source), not cached: "mx-auto"
  ! text unit looks untranslated (identical to source), not cached: "Short and long spellings, side by side"
  ! text unit looks untranslated (identical to source), not cached: "padding-small, margin-bottom-x-small"
  ! text unit looks untranslated (identical to source), not cached: "padding-large"
  ! text unit looks untranslated (identical to source), not cached: "\n      Top-rated films\n    "
  ! text unit looks untranslated (identical to source), not cached: "Rank"
  ! text unit looks untranslated (identical to source), not cached: "Title"
  ! text unit looks untranslated (identical to source), not cached: "Year"
  ! text unit looks untranslated (identical to source), not cached: "Rating"
  ! text unit looks untranslated (identical to source), not cached: "The Shawshank Redemption"
  ! text unit looks untranslated (identical to source), not cached: "The Godfather"
  ! text unit looks untranslated (identical to source), not cached: "The Godfather: Part II"
  ! text unit looks untranslated (identical to source), not cached: "\n      Films (stacked)\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n        Overview\n      "
  ! text unit looks untranslated (identical to source), not cached: "Details"
  ! text unit looks untranslated (identical to source), not cached: "\n        Disabled\n      "
  ! text unit looks untranslated (identical to source), not cached: "History"
  ! text unit looks untranslated (identical to source), not cached: "The Overview tab's content shows here."
  ! text unit looks untranslated (identical to source), not cached: "First tab"
  ! text unit looks untranslated (identical to source), not cached: "\n        Third tab\n      "
  ! text unit looks untranslated (identical to source), not cached: "Fourth tab"
  ! text unit looks untranslated (identical to source), not cached: "The Third tab's content shows here."
  ! text unit looks untranslated (identical to source), not cached: "small"
  ! text unit looks untranslated (identical to source), not cached: "design"
  ! text unit looks untranslated (identical to source), not cached: "large"
  ! text unit looks untranslated (identical to source), not cached: "read only"
  ! text unit looks untranslated (identical to source), not cached: "filter: open"
  ! text unit looks untranslated (identical to source), not cached: "filter: mine"
  ! text unit looks untranslated (identical to source), not cached: "\n    Restore tags\n  "
  ! text unit looks untranslated (identical to source), not cached: "Text sizes"
  ! text unit looks untranslated (identical to source), not cached: "x-small text"
  ! text unit looks untranslated (identical to source), not cached: "small text"
  ! text unit looks untranslated (identical to source), not cached: "medium text (default)"
  ! text unit looks untranslated (identical to source), not cached: "large text"
  ! text unit looks untranslated (identical to source), not cached: "x-large text"
  ! text unit looks untranslated (identical to source), not cached: "Text weight, style, and color"
  ! text unit looks untranslated (identical to source), not cached: "Bold"
  ! text unit looks untranslated (identical to source), not cached: "Italic"
  ! text unit looks untranslated (identical to source), not cached: "Brand"
  ! text unit looks untranslated (identical to source), not cached: "Warning"
  ! text unit looks untranslated (identical to source), not cached: "Bold danger (stacked)"
  ! text unit looks untranslated (identical to source), not cached: "Text content variants"
  ! text unit looks untranslated (identical to source), not cached: "Content small"
  ! text unit looks untranslated (identical to source), not cached: "Legend"
  ! text unit looks untranslated (identical to source), not cached: "Too short."
  ! text unit looks untranslated (identical to source), not cached: "Read-only content."
  ! text unit looks untranslated (identical to source), not cached: "What ships in this package?"
  ! text unit looks untranslated (identical to source), not cached: "Filled variant"
  ! text unit looks untranslated (identical to source), not cached: "\n      The summary reads as an action-se"
  ! text unit looks untranslated (identical to source), not cached: "Chevron at the end"
  ! text unit looks untranslated (identical to source), not cached: "\n      The disclosure chevron sits after"
  ! text unit looks untranslated (identical to source), not cached: "\n      Larger summary font and content p"
  ! text unit looks untranslated (identical to source), not cached: "Advanced settings"
  ! text unit looks untranslated (identical to source), not cached: "\n        These options are revealed when"
  ! text unit looks untranslated (identical to source), not cached: "Billing details"
  ! text unit looks untranslated (identical to source), not cached: "Collapsed by default. Click the summary "
  ! text unit looks untranslated (identical to source), not cached: "Token utilities"
  ! text unit looks untranslated (identical to source), not cached: "\n    Every \"one token, one property\" fam"
  ! text unit looks untranslated (identical to source), not cached: ", elevation\n    "
  ! text unit looks untranslated (identical to source), not cached: ", and "
  ! text unit looks untranslated (identical to source), not cached: "font-weight-body-base"
  ! text unit looks untranslated (identical to source), not cached: "font-weight-body-strong"
  ! text unit looks untranslated (identical to source), not cached: "font-family-code"
  ! text unit looks untranslated (identical to source), not cached: "\n      radius-container-sm\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      radius-container-lg\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      elevation-resting\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      elevation-topmost\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      opacity-disabled\n    "
  ! text unit looks untranslated (identical to source), not cached: "Default placement is top"
  ! text unit looks untranslated (identical to source), not cached: "\n      Hover me\n      "
  ! text unit looks untranslated (identical to source), not cached: "From start"
  ! text unit looks untranslated (identical to source), not cached: "From end"
  ! text unit looks untranslated (identical to source), not cached: "Filters"
  ! text unit looks untranslated (identical to source), not cached: "\n      A tray slides in from the start e"
  ! text unit looks untranslated (identical to source), not cached: "\n      This one docks to the end edge. P"
  ! text unit looks untranslated (identical to source), not cached: " Course files"
  ! text unit looks untranslated (identical to source), not cached: " Syllabus.pdf"
  ! text unit looks untranslated (identical to source), not cached: " Week 1"
  ! text unit looks untranslated (identical to source), not cached: " Reading.pdf"
  ! text unit looks untranslated (identical to source), not cached: " Slides.pptx"
  ! text unit looks untranslated (identical to source), not cached: " Rubric.docx"
  ! text unit looks untranslated (identical to source), not cached: "\n      This single line keeps going past"
  ! text unit looks untranslated (identical to source), not cached: "\n      Set the "
  ! text unit looks untranslated (identical to source), not cached: " custom property to clamp to a fixed num"
  ! text unit looks untranslated (identical to source), not cached: "View"
  ! text unit looks untranslated (identical to source), not cached: " is InstUI's "
  ! text unit looks untranslated (identical to source), not cached: ". Beyond the neutral box it carries\n    "
  ! text unit looks untranslated (identical to source), not cached: "Native modifiers"
  ! text unit looks untranslated (identical to source), not cached: "-background-primary -border-radius-large"
  ! text unit looks untranslated (identical to source), not cached: "-border-width-small -border-color-brand "
  ! text unit looks untranslated (identical to source), not cached: "-background-brand"
  ! text unit looks untranslated (identical to source), not cached: "Compose utilities too"
  ! text unit looks untranslated (identical to source), not cached: "\n    Spacing, color, and token utilities"
  ! text unit looks untranslated (identical to source), not cached: "bg-accent-blue surface with on-color tex"
  ! text unit looks untranslated (identical to source), not cached: "\n      Dismissable with "
  ! text unit looks untranslated (identical to source), not cached: " — I fade out when closed.\n      "
  ! text unit looks untranslated (identical to source), not cached: " — I close instantly.\n      "
  ! text unit looks untranslated (identical to source), not cached: "\n      Not dismissable, and flat via "
  ! text unit looks untranslated (identical to source), not cached: " (alerts are elevated by default).\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      A custom icon via the "
  ! text unit looks untranslated (identical to source), not cached: " class, kept white on the status bar.\n  "
  ! text unit looks untranslated (identical to source), not cached: "\n    Restore alerts\n  "
  ! text unit looks untranslated (identical to source), not cached: "DW"
  ! text unit looks untranslated (identical to source), not cached: "AB"
  ! text unit looks untranslated (identical to source), not cached: "CD"
  ! text unit looks untranslated (identical to source), not cached: "EF"
  ! text unit looks untranslated (identical to source), not cached: "GH"
  ! text unit looks untranslated (identical to source), not cached: "IJ"
  ! text unit looks untranslated (identical to source), not cached: "KL"
  ! text unit looks untranslated (identical to source), not cached: "AI"
  ! text unit looks untranslated (identical to source), not cached: "2XS"
  ! text unit looks untranslated (identical to source), not cached: "XS"
  ! text unit looks untranslated (identical to source), not cached: "SM"
  ! text unit looks untranslated (identical to source), not cached: "MD"
  ! text unit looks untranslated (identical to source), not cached: "LG"
  ! text unit looks untranslated (identical to source), not cached: "XL"
  ! text unit looks untranslated (identical to source), not cached: "2XL"
  ! text unit looks untranslated (identical to source), not cached: "MN"
  ! text unit looks untranslated (identical to source), not cached: "OP"
  ! text unit looks untranslated (identical to source), not cached: "QR"
  ! text unit looks untranslated (identical to source), not cached: "ST"
  ! text unit looks untranslated (identical to source), not cached: "Sarah Robinson"
  ! text unit looks untranslated (identical to source), not cached: "Kyle Montgomery"
  ! text unit looks untranslated (identical to source), not cached: "Alexandria Winterbourne"
  ! text unit looks untranslated (identical to source), not cached: "Miguel"
  ! text unit looks untranslated (identical to source), not cached: "Sanchez"
  ! text unit looks untranslated (identical to source), not cached: "Priya"
  ! text unit looks untranslated (identical to source), not cached: "Inbox"
  ! text unit looks untranslated (identical to source), not cached: "New"
  ! text unit looks untranslated (identical to source), not cached: "Base reset"
  ! text unit looks untranslated (identical to source), not cached: "\n    This result renders with "
  ! text unit looks untranslated (identical to source), not cached: " loaded first. The page surface, base te"
  ! text unit looks untranslated (identical to source), not cached: " all come from the tokens — no rule of y"
  ! text unit looks untranslated (identical to source), not cached: "\n    Bare elements pick up sensible defa"
  ! text unit looks untranslated (identical to source), not cached: "base link"
  ! text unit looks untranslated (identical to source), not cached: ", and everything sits on the token page "
  ! text unit looks untranslated (identical to source), not cached: "background-page"
  ! text unit looks untranslated (identical to source), not cached: "background-base"
  ! text unit looks untranslated (identical to source), not cached: "No items yet"
  ! text unit looks untranslated (identical to source), not cached: "Create your first item to get started."
  ! text unit looks untranslated (identical to source), not cached: "Add a file"
  ! text unit looks untranslated (identical to source), not cached: "Click anywhere in this billboard."
  ! text unit looks untranslated (identical to source), not cached: "Home"
  ! text unit looks untranslated (identical to source), not cached: "Guides"
  ! text unit looks untranslated (identical to source), not cached: "Breadcrumb"
  ! text unit looks untranslated (identical to source), not cached: "Course"
  ! text unit looks untranslated (identical to source), not cached: "Assignment"
  ! text unit looks untranslated (identical to source), not cached: "Dashboard"
  ! text unit looks untranslated (identical to source), not cached: "Reports"
  ! text unit looks untranslated (identical to source), not cached: "Q3 summary"
  ! text unit looks untranslated (identical to source), not cached: "Home Page"
  ! text unit looks untranslated (identical to source), not cached: "Colors"
  ! text unit looks untranslated (identical to source), not cached: "Primary"
  ! text unit looks untranslated (identical to source), not cached: "Secondary"
  ! text unit looks untranslated (identical to source), not cached: "Tertiary"
  ! text unit looks untranslated (identical to source), not cached: "Success"
  ! text unit looks untranslated (identical to source), not cached: "Danger"
  ! text unit looks untranslated (identical to source), not cached: "AI primary"
  ! text unit looks untranslated (identical to source), not cached: "AI secondary"
  ! text unit looks untranslated (identical to source), not cached: "Primary inverse (on a colored surface)"
  ! text unit looks untranslated (identical to source), not cached: "Primary inverse"
  ! text unit looks untranslated (identical to source), not cached: "Ghost (no background)"
  ! text unit looks untranslated (identical to source), not cached: "Primary ghost"
  ! text unit looks untranslated (identical to source), not cached: "Secondary ghost"
  ! text unit looks untranslated (identical to source), not cached: "Block (full width)"
  ! text unit looks untranslated (identical to source), not cached: "Block button"
  ! text unit looks untranslated (identical to source), not cached: "Sizes"
  ! text unit looks untranslated (identical to source), not cached: "Small"
  ! text unit looks untranslated (identical to source), not cached: "Medium"
  ! text unit looks untranslated (identical to source), not cached: "Large"
  ! text unit looks untranslated (identical to source), not cached: "Icons and shapes"
  ! text unit looks untranslated (identical to source), not cached: "\n          Continue\n          "
  ! text unit looks untranslated (identical to source), not cached: "\n          Download\n        "
  ! text unit looks untranslated (identical to source), not cached: "Star"
  ! text unit looks untranslated (identical to source), not cached: "Add"
  ! text unit looks untranslated (identical to source), not cached: "Condensed and states"
  ! text unit looks untranslated (identical to source), not cached: "Condensed"
  ! text unit looks untranslated (identical to source), not cached: "Condensed small"
  ! text unit looks untranslated (identical to source), not cached: "Toggle"
  ! text unit looks untranslated (identical to source), not cached: "Disabled"
  ! text unit looks untranslated (identical to source), not cached: "What's new"
  ! text unit looks untranslated (identical to source), not cached: "\n          The figure can be any leading"
  ! text unit looks untranslated (identical to source), not cached: "Documentation"
  ! text unit looks untranslated (identical to source), not cached: "\n          With "
  ! text unit looks untranslated (identical to source), not cached: " the figure pins to the top instead of c"
  ! text unit looks untranslated (identical to source), not cached: "Compact"
  ! text unit looks untranslated (identical to source), not cached: " constrains the max width."
  ! text unit looks untranslated (identical to source), not cached: "March 2026"
  ! text unit looks untranslated (identical to source), not cached: "Su"
  ! text unit looks untranslated (identical to source), not cached: "Mo"
  ! text unit looks untranslated (identical to source), not cached: "Tu"
  ! text unit looks untranslated (identical to source), not cached: "We"
  ! text unit looks untranslated (identical to source), not cached: "Th"
  ! text unit looks untranslated (identical to source), not cached: "Fr"
  ! text unit looks untranslated (identical to source), not cached: "Sa"
  ! text unit looks untranslated (identical to source), not cached: " Checked"
  ! text unit looks untranslated (identical to source), not cached: " Unchecked"
  ! text unit looks untranslated (identical to source), not cached: " Indeterminate"
  ! text unit looks untranslated (identical to source), not cached: " Small"
  ! text unit looks untranslated (identical to source), not cached: " Error state"
  ! text unit looks untranslated (identical to source), not cached: " Disabled"
  ! text unit looks untranslated (identical to source), not cached: " Toggle switch\n    "
  ! text unit looks untranslated (identical to source), not cached: " Label on top\n      "
  ! text unit looks untranslated (identical to source), not cached: " Label at start\n      "
  ! text unit looks untranslated (identical to source), not cached: " Label at end\n      "
  ! text unit looks untranslated (identical to source), not cached: "Semantic color"
  ! text unit looks untranslated (identical to source), not cached: " paints text, "
  ! text unit looks untranslated (identical to source), not cached: " a surface, and\n    "
  ! text unit looks untranslated (identical to source), not cached: " a border — each from a semantic color t"
  ! text unit looks untranslated (identical to source), not cached: "text-success"
  ! text unit looks untranslated (identical to source), not cached: "text-warning"
  ! text unit looks untranslated (identical to source), not cached: "text-error"
  ! text unit looks untranslated (identical to source), not cached: "text-info"
  ! text unit looks untranslated (identical to source), not cached: "Surfaces and borders"
  ! text unit looks untranslated (identical to source), not cached: "\n    Accent surfaces pair with "
  ! text unit looks untranslated (identical to source), not cached: "bg-accent-green"
  ! text unit looks untranslated (identical to source), not cached: "bg-accent-plum"
  ! text unit looks untranslated (identical to source), not cached: "border-strong"
  ! text unit looks untranslated (identical to source), not cached: "border-brand"
  ! text unit looks untranslated (identical to source), not cached: "Button"
  ! text unit looks untranslated (identical to source), not cached: "Published"
  ! text unit looks untranslated (identical to source), not cached: "tag"
  ! text unit looks untranslated (identical to source), not cached: "dismissible"
  ! text unit looks untranslated (identical to source), not cached: "An alert, styled from the tokens."
  ! text unit looks untranslated (identical to source), not cached: "Active users"
  ! text unit looks untranslated (identical to source), not cached: " Custom checkbox"
  ! text unit looks untranslated (identical to source), not cached: " Toggle"
  ! text unit looks untranslated (identical to source), not cached: " Radio"
  ! text unit looks untranslated (identical to source), not cached: "\n    Show details\n  "
  ! text unit looks untranslated (identical to source), not cached: "\n    A context view frames a callout wit"
  ! text unit looks untranslated (identical to source), not cached: "Placement top"
  ! text unit looks untranslated (identical to source), not cached: "Placement bottom"
  ! text unit looks untranslated (identical to source), not cached: "Placement start"
  ! text unit looks untranslated (identical to source), not cached: "Placement end"
  ! text unit looks untranslated (identical to source), not cached: "Inverse background"
  ! text unit looks untranslated (identical to source), not cached: "\n    Positioning next to a trigger uses "
  ! text unit looks untranslated (identical to source), not cached: "Due date"
  ! text unit looks untranslated (identical to source), not cached: "Starts at"
  ! text unit looks untranslated (identical to source), not cached: "Reminder time"
  ! text unit looks untranslated (identical to source), not cached: "Time zone"
  ! text unit looks untranslated (identical to source), not cached: "Pacific"
  ! text unit looks untranslated (identical to source), not cached: "Mountain"
  ! text unit looks untranslated (identical to source), not cached: "Central"
  ! text unit looks untranslated (identical to source), not cached: "Eastern"
  ! text unit looks untranslated (identical to source), not cached: "\n      Toggle panel\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Open\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Close\n    "
  ! text unit looks untranslated (identical to source), not cached: "Navigation"
  ! text unit looks untranslated (identical to source), not cached: "Modules"
  ! text unit looks untranslated (identical to source), not cached: "Settings"
  ! text unit looks untranslated (identical to source), not cached: "Main content"
  ! text unit looks untranslated (identical to source), not cached: "\n          Use the buttons above to togg"
  ! text unit looks untranslated (identical to source), not cached: "Elevation levels"
  ! text unit looks untranslated (identical to source), not cached: "resting"
  ! text unit looks untranslated (identical to source), not cached: "above"
  ! text unit looks untranslated (identical to source), not cached: "topmost"
  ! text unit looks untranslated (identical to source), not cached: "InstUI aliases"
  ! text unit looks untranslated (identical to source), not cached: "depth1"
  ! text unit looks untranslated (identical to source), not cached: "depth2"
  ! text unit looks untranslated (identical to source), not cached: "depth3"
  ! text unit looks untranslated (identical to source), not cached: "card"
  ! text unit looks untranslated (identical to source), not cached: "cardHover"
  ! text unit looks untranslated (identical to source), not cached: "Drag an image here"
  ! text unit looks untranslated (identical to source), not cached: ", or click to browse.\n      "
  ! text unit looks untranslated (identical to source), not cached: "\n        PNG or JPG up to 5&nbsp;MB.\n   "
  ! text unit looks untranslated (identical to source), not cached: " photo.png — accepted.\n    "
  ! text unit looks untranslated (identical to source), not cached: " report.pdf — file type not allowed.\n   "
  ! text unit looks untranslated (identical to source), not cached: "\n      Tab through the controls (or clic"
  ! text unit looks untranslated (identical to source), not cached: "; the modifiers recolour it, inset it, o"
  ! text unit looks untranslated (identical to source), not cached: "Default ring (any focusable)"
  ! text unit looks untranslated (identical to source), not cached: "Link"
  ! text unit looks untranslated (identical to source), not cached: "Ring colour"
  ! text unit looks untranslated (identical to source), not cached: "Info (default)"
  ! text unit looks untranslated (identical to source), not cached: "Position and animation"
  ! text unit looks untranslated (identical to source), not cached: "Inset ring"
  ! text unit looks untranslated (identical to source), not cached: "No animation"
  ! text unit looks untranslated (identical to source), not cached: "Focus within (reveals on click too)"
  ! text unit looks untranslated (identical to source), not cached: "Search"
  ! text unit looks untranslated (identical to source), not cached: "Email address"
  ! text unit looks untranslated (identical to source), not cached: "We'll never share it."
  ! text unit looks untranslated (identical to source), not cached: "Enter a valid email address."
  ! text unit looks untranslated (identical to source), not cached: "Username"
  ! text unit looks untranslated (identical to source), not cached: "Must be at least 3 characters."
  ! text unit looks untranslated (identical to source), not cached: "Nickname"
  ! text unit looks untranslated (identical to source), not cached: "Account ID"
  ! text unit looks untranslated (identical to source), not cached: "Shipping address"
  ! text unit looks untranslated (identical to source), not cached: "First name"
  ! text unit looks untranslated (identical to source), not cached: "Last name"
  ! text unit looks untranslated (identical to source), not cached: "City"
  ! text unit looks untranslated (identical to source), not cached: "CA"
  ! text unit looks untranslated (identical to source), not cached: "NY"
  ! text unit looks untranslated (identical to source), not cached: "TX"
  ! text unit looks untranslated (identical to source), not cached: "All fields are used for delivery only."
  ! text unit looks untranslated (identical to source), not cached: "Hint: use 8+ characters."
  ! text unit looks untranslated (identical to source), not cached: "Error: this field is required."
  ! text unit looks untranslated (identical to source), not cached: "Success: username is available."
  ! text unit looks untranslated (identical to source), not cached: "\n      Screen-reader-only: form submitte"
  ! text unit looks untranslated (identical to source), not cached: "Gap"
  ! text unit looks untranslated (identical to source), not cached: "\n    Flex/grid "
  ! text unit looks untranslated (identical to source), not cached: " classes on the spacing scale, short ("
  ! text unit looks untranslated (identical to source), not cached: ") or long\n    ("
  ! text unit looks untranslated (identical to source), not cached: ") spelling — usable bare or chained onto"
  ! text unit looks untranslated (identical to source), not cached: "gap-sm"
  ! text unit looks untranslated (identical to source), not cached: "One"
  ! text unit looks untranslated (identical to source), not cached: "Two"
  ! text unit looks untranslated (identical to source), not cached: "Long spelling"
  ! text unit looks untranslated (identical to source), not cached: "gap-large"
  ! text unit looks untranslated (identical to source), not cached: "Heading levels"
  ! text unit looks untranslated (identical to source), not cached: "Heading h1"
  ! text unit looks untranslated (identical to source), not cached: "Heading h2"
  ! text unit looks untranslated (identical to source), not cached: "Heading h3"
  ! text unit looks untranslated (identical to source), not cached: "Heading h4"
  ! text unit looks untranslated (identical to source), not cached: "Heading h5"
  ! text unit looks untranslated (identical to source), not cached: "Heading h6"
  ! text unit looks untranslated (identical to source), not cached: "Heading type variants"
  ! text unit looks untranslated (identical to source), not cached: "Title page"
  ! text unit looks untranslated (identical to source), not cached: "Title section"
  ! text unit looks untranslated (identical to source), not cached: "Title card section"
  ! text unit looks untranslated (identical to source), not cached: "Title card regular"
  ! text unit looks untranslated (identical to source), not cached: "Title card mini"
  ! text unit looks untranslated (identical to source), not cached: "Label"
  ! text unit looks untranslated (identical to source), not cached: "Heading color and treatment"
  ! text unit looks untranslated (identical to source), not cached: "Primary (default)"
  ! text unit looks untranslated (identical to source), not cached: "AI gradient"
  ! text unit looks untranslated (identical to source), not cached: "With bottom border"
  ! text unit looks untranslated (identical to source), not cached: "\n      Inline "
  ! text unit looks untranslated (identical to source), not cached: " icons scale to the text and inherit\n   "
  ! text unit looks untranslated (identical to source), not cached: "\n      Add\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Project name:\n      "
  ! text unit looks untranslated (identical to source), not cached: "Untitled"
  ! text unit looks untranslated (identical to source), not cached: "\n      Owner:\n      "
  ! text unit looks untranslated (identical to source), not cached: "Ada Byron"
  ! text unit looks untranslated (identical to source), not cached: "\n      Created:\n      "
  ! text unit looks untranslated (identical to source), not cached: "March 12, 2026"
  ! text unit looks untranslated (identical to source), not cached: "\n    Click a value to edit it. The state"
  ! text unit looks untranslated (identical to source), not cached: "Layout utilities"
  ! text unit looks untranslated (identical to source), not cached: " and "
  ! text unit looks untranslated (identical to source), not cached: " cover InstUI's cross-cutting\n    "
  ! text unit looks untranslated (identical to source), not cached: " props as composable classes — reach for"
  ! text unit looks untranslated (identical to source), not cached: "Display"
  ! text unit looks untranslated (identical to source), not cached: " lays these chips out in a row; each chi"
  ! text unit looks untranslated (identical to source), not cached: " one "
  ! text unit looks untranslated (identical to source), not cached: " two "
  ! text unit looks untranslated (identical to source), not cached: " three "
  ! text unit looks untranslated (identical to source), not cached: "Text align"
  ! text unit looks untranslated (identical to source), not cached: "text-align-start"
  ! text unit looks untranslated (identical to source), not cached: "text-align-center"
  ! text unit looks untranslated (identical to source), not cached: "text-align-end"
  ! text unit looks untranslated (identical to source), not cached: "A styled link"
  ! text unit looks untranslated (identical to source), not cached: "A large link"
  ! text unit looks untranslated (identical to source), not cached: "\n      Icon at the start\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Icon at the end\n      "
  ! text unit looks untranslated (identical to source), not cached: "\n      Read the "
  ! text unit looks untranslated (identical to source), not cached: "inline documentation"
  ! text unit looks untranslated (identical to source), not cached: " for the full story.\n    "
  ! text unit looks untranslated (identical to source), not cached: "Unstyled link"
  ! text unit looks untranslated (identical to source), not cached: "A link on a dark background"
  ! text unit looks untranslated (identical to source), not cached: "\n      With a leading icon\n    "
  ! text unit looks untranslated (identical to source), not cached: "First item"
  ! text unit looks untranslated (identical to source), not cached: "Second item"
  ! text unit looks untranslated (identical to source), not cached: "Third item"
  ! text unit looks untranslated (identical to source), not cached: "Solid delimiter"
  ! text unit looks untranslated (identical to source), not cached: "Between each"
  ! text unit looks untranslated (identical to source), not cached: "List item"
  ! text unit looks untranslated (identical to source), not cached: "Ordered"
  ! text unit looks untranslated (identical to source), not cached: "Dashed"
  ! text unit looks untranslated (identical to source), not cached: "Items"
  ! text unit looks untranslated (identical to source), not cached: "Content behind the mask."
  ! text unit looks untranslated (identical to source), not cached: "Actions"
  ! text unit looks untranslated (identical to source), not cached: "Edit"
  ! text unit looks untranslated (identical to source), not cached: "Duplicate"
  ! text unit looks untranslated (identical to source), not cached: "Delete"
  ! text unit looks untranslated (identical to source), not cached: "Uptime"
  ! text unit looks untranslated (identical to source), not cached: "3.2s"
  ! text unit looks untranslated (identical to source), not cached: "Avg. response"
  ! text unit looks untranslated (identical to source), not cached: "Start"
  ! text unit looks untranslated (identical to source), not cached: "-text-align-start"
  ! text unit looks untranslated (identical to source), not cached: "Center"
  ! text unit looks untranslated (identical to source), not cached: "-text-align-center"
  ! text unit looks untranslated (identical to source), not cached: "End"
  ! text unit looks untranslated (identical to source), not cached: "-text-align-end"
  ! text unit looks untranslated (identical to source), not cached: "\n      Small\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Medium\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Large\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Auto\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Full screen\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      Media\n    "
  ! text unit looks untranslated (identical to source), not cached: " — a narrow modal."
  ! text unit looks untranslated (identical to source), not cached: "Close"
  ! text unit looks untranslated (identical to source), not cached: "The default size — no modifier needed."
  ! text unit looks untranslated (identical to source), not cached: " — a wider modal for denser content."
  ! text unit looks untranslated (identical to source), not cached: "Auto"
  ! text unit looks untranslated (identical to source), not cached: " sizes to its content."
  ! text unit looks untranslated (identical to source), not cached: "Full screen"
  ! text unit looks untranslated (identical to source), not cached: " fills the viewport."
  ! text unit looks untranslated (identical to source), not cached: "Media"
  ! text unit looks untranslated (identical to source), not cached: "Page"
  ! text unit looks untranslated (identical to source), not cached: "of 12"
  ! text unit looks untranslated (identical to source), not cached: "Draft"
  ! text unit looks untranslated (identical to source), not cached: "In review"
  ! text unit looks untranslated (identical to source), not cached: "Stale"
  ! text unit looks untranslated (identical to source), not cached: "Blocked"
  ! text unit looks untranslated (identical to source), not cached: "\n    Open popover\n  "
  ! text unit looks untranslated (identical to source), not cached: "Share this page"
  ! text unit looks untranslated (identical to source), not cached: "\n      A popover is a lightweight surfac"
  ! text unit looks untranslated (identical to source), not cached: " attribute.\n    "
  ! text unit looks untranslated (identical to source), not cached: "Animated after a 500ms delay"
  ! text unit looks untranslated (identical to source), not cached: "Heading level one"
  ! text unit looks untranslated (identical to source), not cached: "Heading level two"
  ! text unit looks untranslated (identical to source), not cached: "Heading level three"
  ! text unit looks untranslated (identical to source), not cached: "Heading level four"
  ! text unit looks untranslated (identical to source), not cached: "Heading level five"
  ! text unit looks untranslated (identical to source), not cached: "Heading level six"
  ! text unit looks untranslated (identical to source), not cached: "\n      Body copy is InstUI's Text "
  ! text unit looks untranslated (identical to source), not cached: " style. A paragraph with\n      "
  ! text unit looks untranslated (identical to source), not cached: "a link"
  ! text unit looks untranslated (identical to source), not cached: "bold text"
  ! text unit looks untranslated (identical to source), not cached: "emphasis"
  ! text unit looks untranslated (identical to source), not cached: "small print"
  ! text unit looks untranslated (identical to source), not cached: ", and some "
  ! text unit looks untranslated (identical to source), not cached: ". The prose layer styles rendered\n      "
  ! text unit looks untranslated (identical to source), not cached: "A blockquote, styled from the Text conte"
  ! text unit looks untranslated (identical to source), not cached: "Bucket"
  ! text unit looks untranslated (identical to source), not cached: "@pantoken/css"
  ! text unit looks untranslated (identical to source), not cached: "formats"
  ! text unit looks untranslated (identical to source), not cached: "@pantoken/react"
  ! text unit looks untranslated (identical to source), not cached: "renderers"
  ! text unit looks untranslated (identical to source), not cached: "Standard"
  ! text unit looks untranslated (identical to source), not cached: " Option A"
  ! text unit looks untranslated (identical to source), not cached: " Option B"
  ! text unit looks untranslated (identical to source), not cached: " Large"
  ! text unit looks untranslated (identical to source), not cached: "Toggle buttons (variant=\"toggle\")"
  ! text unit looks untranslated (identical to source), not cached: " Medium"
  ! text unit looks untranslated (identical to source), not cached: "Toggle context colors"
  ! text unit looks untranslated (identical to source), not cached: " Success"
  ! text unit looks untranslated (identical to source), not cached: " Danger"
  ! text unit looks untranslated (identical to source), not cached: " Warning"
  ! text unit looks untranslated (identical to source), not cached: " Off"
  ! text unit looks untranslated (identical to source), not cached: "T-shirt size"
  ! text unit looks untranslated (identical to source), not cached: " Small\n      "
  ! text unit looks untranslated (identical to source), not cached: " Medium\n      "
  ! text unit looks untranslated (identical to source), not cached: " Large "
  ! text unit looks untranslated (identical to source), not cached: "Notification frequency"
  ! text unit looks untranslated (identical to source), not cached: " Real time"
  ! text unit looks untranslated (identical to source), not cached: " Daily digest"
  ! text unit looks untranslated (identical to source), not cached: " Weekly digest"
  ! text unit looks untranslated (identical to source), not cached: "Contact method"
  ! text unit looks untranslated (identical to source), not cached: " Email"
  ! text unit looks untranslated (identical to source), not cached: " Phone"
  ! text unit looks untranslated (identical to source), not cached: " Mail"
  ! text unit looks untranslated (identical to source), not cached: "Grading range"
  ! text unit looks untranslated (identical to source), not cached: "\n      Narrow layout — hidden at "
  ! text unit looks untranslated (identical to source), not cached: " (48rem) and up via "
  ! text unit looks untranslated (identical to source), not cached: "\n      Wide layout — hidden at "
  ! text unit looks untranslated (identical to source), not cached: " and below via "
  ! text unit looks untranslated (identical to source), not cached: "\n      Breakpoints: "
  ! text unit looks untranslated (identical to source), not cached: " 30rem, "
  ! text unit looks untranslated (identical to source), not cached: " 48rem, "
  ! text unit looks untranslated (identical to source), not cached: " 64rem,\n      "
  ! text unit looks untranslated (identical to source), not cached: " 80rem. For a component that adapts to i"
  ! text unit looks untranslated (identical to source), not cached: " element and use\n      "
  ! text unit looks untranslated (identical to source), not cached: " queries with the same widths.\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n  This sentence is visible. After it si"
  ! text unit looks untranslated (identical to source), not cached: "\n    This text is announced to assistive"
  ! text unit looks untranslated (identical to source), not cached: "\n  Switch to the HTML tab to see it in t"
  ! text unit looks untranslated (identical to source), not cached: "Calendar"
  ! text unit looks untranslated (identical to source), not cached: "Page content sits beside the rail."
  ! text unit looks untranslated (identical to source), not cached: "Choose a fruit…"
  ! text unit looks untranslated (identical to source), not cached: "Apple"
  ! text unit looks untranslated (identical to source), not cached: "Orange"
  ! text unit looks untranslated (identical to source), not cached: "Pear"
  ! text unit looks untranslated (identical to source), not cached: "Invalid selection"
  ! text unit looks untranslated (identical to source), not cached: "Spacing"
  ! text unit looks untranslated (identical to source), not cached: "\n    Per-side classes on the spacing sca"
  ! text unit looks untranslated (identical to source), not cached: " for margin/padding, an\n    optional log"
  ! text unit looks untranslated (identical to source), not cached: "),\n    then a step ("
  ! text unit looks untranslated (identical to source), not cached: ", plus "
  ! text unit looks untranslated (identical to source), not cached: " for\n    margin). Every class also has a"
  ! text unit looks untranslated (identical to source), not cached: " is the same\n    rule as "
  ! text unit looks untranslated (identical to source), not cached: "p-xs, mb-2xs"
  ! text unit looks untranslated (identical to source), not cached: "p-sm, mb-2xs"
  ! text unit looks untranslated (identical to source), not cached: "p-lg"
  ! text unit looks untranslated (identical to source), not cached: "Center with auto margins"
  ! text unit looks untranslated (identical to source), not cached: "mx-auto"
  ! text unit looks untranslated (identical to source), not cached: "Short and long spellings, side by side"
  ! text unit looks untranslated (identical to source), not cached: "padding-small, margin-bottom-x-small"
  ! text unit looks untranslated (identical to source), not cached: "padding-large"
  ! text unit looks untranslated (identical to source), not cached: "\n      Top-rated films\n    "
  ! text unit looks untranslated (identical to source), not cached: "Rank"
  ! text unit looks untranslated (identical to source), not cached: "Title"
  ! text unit looks untranslated (identical to source), not cached: "Year"
  ! text unit looks untranslated (identical to source), not cached: "Rating"
  ! text unit looks untranslated (identical to source), not cached: "The Shawshank Redemption"
  ! text unit looks untranslated (identical to source), not cached: "The Godfather"
  ! text unit looks untranslated (identical to source), not cached: "The Godfather: Part II"
  ! text unit looks untranslated (identical to source), not cached: "\n      Films (stacked)\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n        Overview\n      "
  ! text unit looks untranslated (identical to source), not cached: "Details"
  ! text unit looks untranslated (identical to source), not cached: "\n        Disabled\n      "
  ! text unit looks untranslated (identical to source), not cached: "History"
  ! text unit looks untranslated (identical to source), not cached: "The Overview tab's content shows here."
  ! text unit looks untranslated (identical to source), not cached: "First tab"
  ! text unit looks untranslated (identical to source), not cached: "\n        Third tab\n      "
  ! text unit looks untranslated (identical to source), not cached: "Fourth tab"
  ! text unit looks untranslated (identical to source), not cached: "The Third tab's content shows here."
  ! text unit looks untranslated (identical to source), not cached: "small"
  ! text unit looks untranslated (identical to source), not cached: "design"
  ! text unit looks untranslated (identical to source), not cached: "large"
  ! text unit looks untranslated (identical to source), not cached: "read only"
  ! text unit looks untranslated (identical to source), not cached: "filter: open"
  ! text unit looks untranslated (identical to source), not cached: "filter: mine"
  ! text unit looks untranslated (identical to source), not cached: "\n    Restore tags\n  "
  ! text unit looks untranslated (identical to source), not cached: "Text sizes"
  ! text unit looks untranslated (identical to source), not cached: "x-small text"
  ! text unit looks untranslated (identical to source), not cached: "small text"
  ! text unit looks untranslated (identical to source), not cached: "medium text (default)"
  ! text unit looks untranslated (identical to source), not cached: "large text"
  ! text unit looks untranslated (identical to source), not cached: "x-large text"
  ! text unit looks untranslated (identical to source), not cached: "Text weight, style, and color"
  ! text unit looks untranslated (identical to source), not cached: "Bold"
  ! text unit looks untranslated (identical to source), not cached: "Italic"
  ! text unit looks untranslated (identical to source), not cached: "Brand"
  ! text unit looks untranslated (identical to source), not cached: "Warning"
  ! text unit looks untranslated (identical to source), not cached: "Bold danger (stacked)"
  ! text unit looks untranslated (identical to source), not cached: "Text content variants"
  ! text unit looks untranslated (identical to source), not cached: "Content small"
  ! text unit looks untranslated (identical to source), not cached: "Legend"
  ! text unit looks untranslated (identical to source), not cached: "Too short."
  ! text unit looks untranslated (identical to source), not cached: "Read-only content."
  ! text unit looks untranslated (identical to source), not cached: "What ships in this package?"
  ! text unit looks untranslated (identical to source), not cached: "Filled variant"
  ! text unit looks untranslated (identical to source), not cached: "\n      The summary reads as an action-se"
  ! text unit looks untranslated (identical to source), not cached: "Chevron at the end"
  ! text unit looks untranslated (identical to source), not cached: "\n      The disclosure chevron sits after"
  ! text unit looks untranslated (identical to source), not cached: "\n      Larger summary font and content p"
  ! text unit looks untranslated (identical to source), not cached: "Advanced settings"
  ! text unit looks untranslated (identical to source), not cached: "\n        These options are revealed when"
  ! text unit looks untranslated (identical to source), not cached: "Billing details"
  ! text unit looks untranslated (identical to source), not cached: "Collapsed by default. Click the summary "
  ! text unit looks untranslated (identical to source), not cached: "Token utilities"
  ! text unit looks untranslated (identical to source), not cached: "\n    Every \"one token, one property\" fam"
  ! text unit looks untranslated (identical to source), not cached: ", elevation\n    "
  ! text unit looks untranslated (identical to source), not cached: ", and "
  ! text unit looks untranslated (identical to source), not cached: "font-weight-body-base"
  ! text unit looks untranslated (identical to source), not cached: "font-weight-body-strong"
  ! text unit looks untranslated (identical to source), not cached: "font-family-code"
  ! text unit looks untranslated (identical to source), not cached: "\n      radius-container-sm\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      radius-container-lg\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      elevation-resting\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      elevation-topmost\n    "
  ! text unit looks untranslated (identical to source), not cached: "\n      opacity-disabled\n    "
  ! text unit looks untranslated (identical to source), not cached: "Default placement is top"
  ! text unit looks untranslated (identical to source), not cached: "\n      Hover me\n      "
  ! text unit looks untranslated (identical to source), not cached: "From start"
  ! text unit looks untranslated (identical to source), not cached: "From end"
  ! text unit looks untranslated (identical to source), not cached: "Filters"
  ! text unit looks untranslated (identical to source), not cached: "\n      A tray slides in from the start e"
  ! text unit looks untranslated (identical to source), not cached: "\n      This one docks to the end edge. P"
  ! text unit looks untranslated (identical to source), not cached: " Course files"
  ! text unit looks untranslated (identical to source), not cached: " Syllabus.pdf"
  ! text unit looks untranslated (identical to source), not cached: " Week 1"
  ! text unit looks untranslated (identical to source), not cached: " Reading.pdf"
  ! text unit looks untranslated (identical to source), not cached: " Slides.pptx"
  ! text unit looks untranslated (identical to source), not cached: " Rubric.docx"
  ! text unit looks untranslated (identical to source), not cached: "\n      This single line keeps going past"
  ! text unit looks untranslated (identical to source), not cached: "\n      Set the "
  ! text unit looks untranslated (identical to source), not cached: " custom property to clamp to a fixed num"
  ! text unit looks untranslated (identical to source), not cached: "View"
  ! text unit looks untranslated (identical to source), not cached: " is InstUI's "
  ! text unit looks untranslated (identical to source), not cached: ". Beyond the neutral box it carries\n    "
  ! text unit looks untranslated (identical to source), not cached: "Native modifiers"
  ! text unit looks untranslated (identical to source), not cached: "-background-primary -border-radius-large"
  ! text unit looks untranslated (identical to source), not cached: "-border-width-small -border-color-brand "
  ! text unit looks untranslated (identical to source), not cached: "-background-brand"
  ! text unit looks untranslated (identical to source), not cached: "Compose utilities too"
  ! text unit looks untranslated (identical to source), not cached: "\n    Spacing, color, and token utilities"
  ! text unit looks untranslated (identical to source), not cached: "bg-accent-blue surface with on-color tex"

---
vp run: @pantoken/docs#docs:demos:locales not cached because it modified its input. (Run `vp run --last-details` for full details)

---

now that we're in a folder, for each of the demos take the inline style tag and move it to a standalone css file and the script tag and move it to a standalone file, then update the demo builder to inject them and the demo preview tools should still show the tabs for html/css/js - if they're there.

---

css and js should also get parsed for i18n handlebars so things like comments and html injection are still localized.

---

we shouldn't be hard-coding any translations expectations into tests.