/**
 * Re-exports the canonical token type contracts from `@pantoken/model`, the zero-dependency home
 * of the IR types. Core files import from here so the type source stays single.
 *
 * @module
 */
export type { Theme, Token, TokenInput, TokenMeta, TokenModify } from "@pantoken/model";
