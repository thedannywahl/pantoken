import { Icon } from "@pantoken/react";

export function App() {
  return (
    <main style={{ padding: "var(--instui-spacing-space-lg)" }}>
      <h1 style={{ color: "var(--instui-color-text-primary)" }}>{"{{projectName}}"}</h1>
      <p>
        Styled with <code>@pantoken/react</code> and <code>@pantoken/css</code> — Instructure design
        tokens and icons.
      </p>
      <Icon name="check-mark" />
    </main>
  );
}
