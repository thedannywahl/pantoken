export default function Page() {
  return (
    <main style={{ padding: "var(--instui-spacing-space-lg)" }}>
      <h1 style={{ color: "var(--instui-color-text-primary)" }}>{"{{projectName}}"}</h1>
      <p>
        Styled with <code>@pantoken/next</code> — Instructure design tokens and icons.
      </p>
    </main>
  );
}
