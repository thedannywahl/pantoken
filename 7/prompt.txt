clicking links in the canvas-theme-editor scaffold preview can go haywire if they're relative anchors `#foo` and cause the page to iframe itself.

---

Read-only discovery, medium thoroughness. In /Users/dwahl/Scripts/pantoken, investigate the bug: clicking links in the canvas-theme-editor scaffold preview can go haywire when href is a relative fragment anchor like #foo and cause the page to iframe itself. Follow repo instructions: FIRST run `entire graph search --repo . --profile full --query "canvas-theme-editor scaffold preview relative fragment links iframe the page itself"`; also run `entire status` to assess checkpoint history. Open the top graph hit and trace only the owning preview/link-navigation implementation, relevant scaffold/generated source, and nearby tests. Check git history / Entire checkpoint intent for key files when available, using noninteractive JSON/no-pager forms. Identify: (1) exact root cause/control flow; (2) smallest correct behavior change, including desired handling of #foo versus external/internal URLs; (3) specific files/symbols to modify; (4) existing tests and exact regression cases; (5) generation or sync steps if scaffold files are generated; (6) validation commands under this repo's vp conventions; (7) whether docs, changeset, ASSURANCE/SECURITY, or ai assets need updates. Do not edit any files. Return concise evidence with workspace-relative paths and line numbers.