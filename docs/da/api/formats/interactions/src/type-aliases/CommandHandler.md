[pantoken](../../../../index.md) / [formats/interactions/src](../index.md) / CommandHandler

# Type Alias: CommandHandler

> **CommandHandler** = (`command`, `source`) => `void`

Callback påkaldt, når en `command` begivenhed (eller fallback klik) løses til et mål.

## Parameters

### command

`string`

### source

`Element` \| `null`

## Returns

`void`
