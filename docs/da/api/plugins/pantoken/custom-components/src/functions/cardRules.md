[pantoken](../../../../../index.md) / [plugins/pantoken/custom-components/src](../index.md) / cardRules

# Funktion: cardRules()

> **cardRules**(`prefix?`): `string`

<span class="instui-pill -color-warning pantoken-doc-tag">Beta</span>

Byg kort-layout CSS (dokkommentar + regler), erstatter `prefix` med `pfx-` sentinel.
Standard til `"instui-"` for det leverede stylesheet; send `""` for et build uden præfiks.

## Parametre

### prefix?

`string` = `"instui-"`

## Returnerer

`string`
