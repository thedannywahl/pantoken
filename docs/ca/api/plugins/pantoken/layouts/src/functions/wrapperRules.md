[pantoken](../../../../../index.md) / [plugins/pantoken/layouts/src](../index.md) / wrapperRules

# Function: wrapperRules()

> **wrapperRules**(`prefix?`): `string`

<span class="instui-pill -color-warning pantoken-doc-tag">Beta</span>

Construeix el CSS del disseny embolcall (comentari doc + regles), substituint `prefix` per la sentinella `pfx-`.
Per defecte a `"instui-"` per a la fulla d'estils enviada; passa `""` per a una compilació sense prefix.

## Parameters

### prefix?

`string` = `"instui-"`

## Returns

`string`
