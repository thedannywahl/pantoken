[pantoken](../../../../index.md) / [renderers/tinymce/src](../index.md) / createTemplatesPlugin

# Funció: createTemplatesPlugin()

> **createTemplatesPlugin**(`options`): (`editor`) => `object`

Construeix la devolució de crida `tinymce.PluginManager.add` per al connector "Inserir plantilla".

## Paràmetres

### options

`TemplatesPluginOptions`

## Retorna

(`editor`) => `object`
