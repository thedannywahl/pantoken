[pantoken](../../../../index.md) / [formats/interactions/src](../index.md) / syncInvoker

# Function: syncInvoker()

> **syncInvoker**(`host`): `void`

Replica els atributs del criador del host a les propietats IDL del seu `&lt;button&gt;` intern, resolent els ids contra l'arrel del host perquè un botó shadow-DOM pugui conduir un target `[popover]`/command de light-DOM.

## Parameters

### host

`HTMLElement`

## Returns

`void`
