[pantoken](../../../../index.md) / [formats/interactions/src](../index.md) / TruncateAutoOptions

# Interfície: TruncateAutoOptions

Botons opcionals per a [initTruncateAuto](../functions/initTruncateAuto.md).

## Propietats

### observeParent?

> `optional` **observeParent?**: `boolean`

Observar la mida del pare del host (per defecte cert).

***

### slot?

> `optional` **slot?**: `HTMLSlotElement` \| `null`

Ranura opcional per observar les resincronitzacions de `slotchange` en l'ús de shadow DOM.
