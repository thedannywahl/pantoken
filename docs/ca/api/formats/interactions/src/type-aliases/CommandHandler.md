[pantoken](../../../../index.md) / [formats/interactions/src](../index.md) / CommandHandler

# Àlies de tipus: CommandHandler

> **CommandHandler** = (`command`, `source`) => `void`

Callback invocat quan un event `command` (o clic de reserva) es resol a un destí.

## Paràmetres

### command

`string`

### source

`Element` \| `null`

## Retorna

`void`
