[pantoken](../../../../index.md) / [formats/components/src](../index.md) / ProseOptions

# Interfície: ProseOptions

<span class="instui-pill -color-warning pantoken-doc-tag">Beta</span>

Opcions per a [proseCss](../functions/proseCss.md).

## Propietats

### scope?

> `optional` **scope?**: `string`

<span class="instui-pill -color-warning pantoken-doc-tag">Beta</span>

El selector de content-root al qual s'adjunten les regles (per defecte `":where(body)"`, aplicat automàticament
com `base.css` — passeu una classe com a `".vp-doc"` per establir l'abast a una content root diferent).
