[pantoken](../../../../index.md) / [packages/utils/src](../index.md) / sanitizeSvg

# Function: sanitizeSvg()

> **sanitizeSvg**(`svg`): `string`

<span class="instui-pill -color-warning pantoken-doc-tag">Beta</span>

Treu elements `&lt;script&gt;` i atributs de controlador d'événements del marcatge SVG.

Defensa en profunditat per a SVG decodificat de URIs de dades venudes o contribuïdes per plugins.
No és un analitzador HTML complet — es basa en que la font ascendent sigui de confiança i fixa.

## Parameters

### svg

`string`

## Returns

`string`
