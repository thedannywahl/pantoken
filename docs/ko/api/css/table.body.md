# CSS: table.body

`tbody` — The table's data row group (InstUI `Table.Body`).

The parent `table`'s `-layout-stacked` modifier restyles this member — see `table`'s own doc for that modifier.

## 사용법

```css
@import "@pantoken/components/components.css";
@import "@pantoken/components/table.body.css";
```

