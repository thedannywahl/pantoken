[pantoken](../../../../index.md) / [formats/interactions/src](../index.md) / TruncateAutoOptions

# Интерфейс: TruncateAutoOptions

Optional knobs for [initTruncateAuto](../functions/initTruncateAuto.md).

## Свойства

### observeParent?

> `optional` **observeParent?**: `boolean`

Observe the host's parent size (default true).

***

### slot?

> `optional` **slot?**: `HTMLSlotElement` \| `null`

Optional slot to watch for `slotchange` re-syncs in shadow DOM usage.
