[pantoken](../../../../index.md) / [tools/demo/src](../index.md) / escapeSrcdoc

# Funzione: escapeSrcdoc()

> **escapeSrcdoc**(`value`): `string`

Escape a string for embedding inside an HTML attribute value delimited by `"`.

## Parametri

### value

`string`

## Restituisce

`string`
