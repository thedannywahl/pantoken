Assets in public directory cannot be imported from JavaScript.
If you intend to import that asset, put the file in the src directory, and use /src/r/registry.json instead of /public/r/registry.json.
If you intend to use the URL of that asset, use /r/registry.json?url.