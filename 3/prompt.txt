between canvas-theme-editor and plugins/tinymce and https://www.tiny.cloud/docs/tinymce/latest/save/

I want pseudo file management so that a user can 'save as...', 'open...' 'save...' or 'delete...' some presets that are saved to local storage. it should include the active theme, the active color, the cdn provider, the current custom css, js, and the content of the tinymce editor.  this is distinct from the auto-save plugin but should work with it.

---

Read-only planning research in /Users/dwahl/Scripts/pantoken. Follow .entire/graph-agent.md strictly: your first locating action must be `entire graph search --repo . --profile full --query "TinyMCE Save plugin pseudo file management presets localStorage distinct from autosave"`. Also run `entire status`, and inspect relevant checkpoint history where available per the using-entire workflow. Focus on plugins/tinymce and its integration with platforms/canvas-theme-editor: locate plugin registration/commands/menu buttons, autosave behavior, APIs to get/set editor content, tests, generated artifacts, docs/i18n, and package boundaries. Determine whether pseudo file management belongs in plugins/tinymce, canvas-theme-editor, or split between them, based on existing architecture. Do not edit anything. Return concise facts with exact paths/symbols, distinguish checkpoint-backed intent from code inference, and list verification commands.

---

Read-only final planning pass in /Users/dwahl/Scripts/pantoken. We are planning a new reusable TinyMCE preset/file-management plugin used by packages/scaffold/templates/canvas-theme-editor. User decisions: TinyMCE toolbar menu; explicit presets only (do not auto-open on reload); unique case-insensitive names with overwrite confirmation; include light/adaptive theme mode. Preset data: active theme, active color, CDN provider, custom CSS, custom JS, TinyMCE HTML. Autosave remains enabled and storage-independent. Follow graph guidance; prior graph search has already been performed in this planning session, so focus narrowly. Identify exact package integration points: root/workspace package discovery, renderer or meta-package exports if appropriate, scaffold dependency generation behavior, i18n/string generation, package tests/configs, changeset/security/doc requirements, and exact focused/full validation commands. Inspect neighboring plugin package patterns and scaffold tests. Do not edit. Return a concise actionable file/symbol list and flag any file that should deliberately not change.