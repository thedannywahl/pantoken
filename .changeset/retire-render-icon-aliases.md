---
"@pantoken/components": major
---

Remove the deprecated `-render-icon-*` and `-render-custom-icon-*` glyph classes and the `deprecatedAliases` option from `iconGlyphsCss`. Replace those classes with `-icon-*`; the shipped `icons.css` now emits only canonical glyph classes.
