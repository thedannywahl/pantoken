---
"@pantoken/plugin-layouts": patch
---

Generate layout translations from the canonical supported-locale registry instead of a copied table, keeping locale bundles in sync when supported languages change.
