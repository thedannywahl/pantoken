---
---

# Tooling only: CI, hooks, and release/docs workflows

Add PR CI, enforce conventional commits, parallelize the `ready` gate, and streamline the release and docs workflows. No published package changes, so no release is needed.
