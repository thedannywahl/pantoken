---
"@pantoken/plugin-custom-components": patch
---

Migrate the custom card component to nested scoped root selectors so `-variant-container` child styling follows the modern alert-style authoring pattern.
