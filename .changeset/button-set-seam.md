---
"@pantoken/plugin-custom-components": minor
---

Show a 1px seam in the buttons' own border color between `button-set` buttons (tinted with the label color on solid fills like primary, whose border matches the fill), and add a `-without-seam` modifier that hides it so the set reads as one continuous control.
