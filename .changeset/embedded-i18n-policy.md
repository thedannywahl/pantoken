---
"@pantoken/i18n-engine": minor
"@pantoken/translation-adapters": minor
---

Moves drift policy into the root `i18n.config.json` schema. The shared drift reporter now derives locale tiers, surface severities, and fallback behavior from that single configuration file.
