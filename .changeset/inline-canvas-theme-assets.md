---
"@pantoken/scaffold": patch
---

Allow the canvas-theme-editor's `None (inline)` provider in production previews and downloads. Keep it as the local-development default while retaining jsDelivr as the production default, and inline dynamically referenced pantoken stylesheets into generated theme CSS.
