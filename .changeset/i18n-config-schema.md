---
"@pantoken/i18n-engine": patch
---

Adds `i18n.config.schema.json` and links the root `i18n.config.json` to it with `$schema`, documenting catalogs, locales, providers, drift policy, and localization spaces for editor tooling.
