---
"@pantoken/components": patch
---

Match glyph class tokens instead of arbitrary `-icon-` substrings in the shared painter and icon-aware component styles, preserving deprecated icon aliases without disturbing Canvas icons.
