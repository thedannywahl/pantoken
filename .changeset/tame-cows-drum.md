---
"@pantoken/components": minor
---

`progress-bar` and `progress-circle` now expose an explicit `-size-md` modifier (with the auto-generated `-size-medium` long-form alias), matching the medium default instead of only being reachable by omitting a size modifier.
