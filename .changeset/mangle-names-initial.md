---
"@pantoken/plugin-mangle-custom-props": patch
---

Initial release. PostCSS plugin that renames `--instui-*` custom properties to minimal sequential identifiers (`base26`, `base36`, or `numeric`), with `sharedManifest` support for cross-file consistent mangling and `propertyMap` for map output via `result.messages`.
