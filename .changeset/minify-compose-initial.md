---
"@pantoken/plugin-props-minify": patch
---

Initial release. Composed custom-property minification utility — re-exports `pruneCustomProps`, `flattenProperty`, and `mangleCustomProps`, defines shared `PropsMinifyOptions`, and exposes `applyMinify(css, opts)` as the single entry point for all pantoken CSS pipelines.
