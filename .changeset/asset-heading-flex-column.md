---
"@pantoken/docs": patch
---

Fix the Agent Tools install-tab asset title/description to stack as a flex column instead of a row
(missing `flex-direction: column` on `.agent-tools-page__asset-heading`).
