---
"@pantoken/components": patch
---

Tag `drawer-layout`'s `-should-overlay-tray` modifier with `@interaction`, matching `alert`'s `-timeout` and `progress-circle`'s `-should-animate` — it's toggled by the `@pantoken/interactions` responsive-overlay behavior, not authored directly.
