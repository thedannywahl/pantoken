---
"@pantoken/pantoken": patch
---

Emit and publish `model.json` from `@pantoken/pantoken` so downstream `cssdoc` configs can use a stable provider model path (for example `@pantoken/pantoken/model.json`) without depending on source stylesheets.
