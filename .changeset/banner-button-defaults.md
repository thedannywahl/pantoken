---
"@pantoken/plugin-custom-components": patch
---

Simplify banner button defaults so only unmodified direct-child buttons receive the on-color primary treatment, while unmodified sibling buttons use a transparent default background.
