---
"@pantoken/ai": patch
---

Add a "Full documentation" section to `AGENTS.md` pointing agents at the hosted docs' `/llms.txt`, `/llms-full.txt`, per-page `.md` mirrors, and `/.well-known/api-catalog` for anything the bundled cheatsheet doesn't cover.
