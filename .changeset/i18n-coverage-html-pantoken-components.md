---
"@pantoken/i18n-engine": patch
---

Rebuild the `i18n stats --html` report on real `@pantoken/components` markup (`table`, `pill`, `text-input`, `simple-select`) and `@pantoken/css` tokens, linked as stylesheets relative to the report's output path, instead of bespoke inline CSS.
