---
"@pantoken/ai": patch
---

Sync the Cursor rule body with the Copilot instructions body at generate time so the two rule files can't drift out of sync, and refresh both with the current `@pantoken/components` package and full CLI target list.
