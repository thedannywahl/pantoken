---
"@pantoken/scaffold": patch
---

The `canvas-theme-editor` scaffold now remembers the author's editor/preview layout direction, preview width, preview fullscreen state, and chosen CDN provider across page reloads, persisted to `localStorage`.
