---
"@pantoken/plugin-custom-components": patch
---

Keep `button-set` on a single row: it no longer wraps its buttons onto separate lines, and neither the set nor its buttons shrink inside a flex parent.
