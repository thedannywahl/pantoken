---
"create-pantoken-app": patch
---

Simplify the README: a shorter blurb, `## Getting started` heading, and a
single `npx create-pantoken-app <platform>` command instead of a hard-coded
example, plus a link to the full docs.
