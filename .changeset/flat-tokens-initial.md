---
"@pantoken/plugin-flatten-property": patch
---

Initial release. PostCSS plugin that converts `@property` at-rules to plain custom-property declarations, with configurable `injectSelector` and `onMissingInitialValue` options.
