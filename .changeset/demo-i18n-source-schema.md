---
"@pantoken/docs": patch
"@pantoken/translation-adapters": major
---

Normalizes demo `i18n.json` sources to the shared `{message, translate}` schema with `$schema` links, and removes the obsolete legacy source parser.
