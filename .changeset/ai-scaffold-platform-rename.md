---
"@pantoken/ai": patch
---

`pantoken-ai scaffold` and `scaffoldAndInit` now support the renamed `html` platform (was `web`) plus the new `angular` and `web-components` platforms, re-exported from the updated `@pantoken/scaffold`.
