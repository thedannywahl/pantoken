---
"@pantoken/pendo": patch
---

Add an escape hatch for Pendo buttons using a custom theme. `._pendo-button-custom` no longer receives InstUI editor-owned visual overrides for colour, background, border, font size, font weight, margin, padding, or hover variants, so the guide author's own styles win while the shared base height, font family, line height, and focus ring still apply.
