id: positive-trigger-001
name: Positive Trigger 1
description: User is starting a brand-new project and wants pantoken wired in from scratch.
tags:
  - positive-trigger
inputs:
  prompt: "I'm starting a new React app and want Instructure's design tokens set up from scratch"
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - "pantoken"
        - "scaffold"
