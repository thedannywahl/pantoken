fallow is failing on ci:
#attachment:Pasted text #1

---

Continue: "Continue to iterate?"

---

you're stuck in an interactive shell, you need to run docs:chrome:locales:copilot instead.

---

is the registry browser strings in the docs translation pipeline? it should be.